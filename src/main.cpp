#include <iostream>
#include <filesystem>
#include <set>
#include <thread>
#include <chrono>
#include <vector>

namespace fs = std::filesystem;

int main() {
    std::vector<std::string> file_paths;
    std::string path;
    std::cout << "Please type your file path:";

    return 0;
}